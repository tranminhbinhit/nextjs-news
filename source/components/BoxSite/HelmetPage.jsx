// /* eslint-disable*/
// import React from 'react';
// import { Helmet } from 'react-helmet';
// import { isEmpty } from '../../../utils/utils';

// const HelmetPage = ({ title, description, image }) => {
//   return (
//     <Helmet>
//       {!isEmpty(title) ? <title>{title}</title> : ''}
//       {!isEmpty(description) ? <meta name="description" content={description} /> : ''}
//       {!isEmpty(title) ? <meta property="og:title" content={title} /> : ''}
//       {!isEmpty(description) ? <meta property="og:description" content={description} /> : ''}
//       {!isEmpty(image) ? <meta property="og:image" content={image} /> : ''}
//     </Helmet>
//   );
// };

// export default HelmetPage;
